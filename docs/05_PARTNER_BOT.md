# Partner Bot

## Назначение

`partner_bot` управляет полным жизненным циклом партнёра:

- регистрация,
- модерация,
- обработка заказов,
- управление профилем и реквизитами.

## Модель статусов партнёра

- `ожидает`
- `активный`
- `приостановлен`
- `отклонён`

Статус хранится в `ServiceDraft.status`.

## Регистрация (FSM)

Партнёр заполняет анкету в `ServiceDraft` по шагам:

- базовые данные сервиса,
- график,
- диагностика,
- опционально банковские реквизиты.

Для типа `complex` анкета требует обе ветки классификации:

- `Категория ремонта`,
- `Категории апгрейда`.

В pending-редактировании драфта доступны только операционные поля анкеты;
пункты `Орг.-правовая форма`, `Налогообложение`, `Банковские реквизиты`
из quick-edit intentionally исключены.

После отправки анкеты:

- создается/обновляется `Service`,
- анкета помечается как ожидающая модерации,
- админам отправляется уведомление.

Перед отправкой анкеты на модерацию в confirm-экране есть кнопка `Назад`.

## Модерация

Админ в `partner_bot.handlers.admin`:

- одобряет,
- отклоняет,
- приостанавливает,
- восстанавливает.

При одобрении данные из draft материализуются в финальный `Service` + `ServiceBankDetails`.
Синхронизация в Google Sheets выполняется в фоне и не блокирует ответ админа.

## Работа с заказами

Партнёр может:

- принять/отклонить заявку,
- перевести в работу и отправить смету,
- при указании итоговой стоимости сначала ввести текст сметы,
- затем указать итоговую сумму,
- автоматически перевести заявку в `ready_for_pickup`,
- отправить клиенту сообщение с кнопкой оплаты (mock).

В карточках заявок партнёр видит данные клиента, включая Telegram username (если доступен).

## Профиль и реквизиты

- Операционные поля (`name/address/phone/hours/...`) редактируются в профиле.
- Для активного партнёра `Редактировать профиль` использует тот же draft-FSM, что и регистрация, поэтому применяются те же валидации и зависимые переходы между полями.
- В активном профиле доступны изменения типа услуг и категорий (`service_type`, `category`, `upgrade_cats`).
- Поле `Telegram` исключено из списка редактируемых полей профиля.
- Банковские поля редактируются отдельно.
- Изменения в активном профиле больше не переводят сервис в повторную модерацию: статусы `Service.partnership_status` и `ServiceDraft.status` остаются `активный`.
- После изменения профиля админам отправляется уведомление с `service_id` и названием изменённого поля.
- Быстрый переключатель статуса (`открыт/пауза/закрыт`) сначала фиксируется в БД,
  затем синхронизируется в Google Sheets асинхронно.

## Временная пауза

- Для паузы используется `pause_until`.
- Фоновый цикл регулярно проверяет истекшие паузы и автоматически открывает сервис.
- Проверка делается в таймзоне Москвы, с защитой для окружений без `tzdata`.

## Уведомления

Настройки уведомлений хранятся в `ServiceOwnerSettings` и управляются через inline toggle.
